<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.common.attach.dao.AttatchDAO">
	<!-- [[개정이력(Modification Information)]] -->
	<!-- 수정일 수정자 수정내용 -->
	<!-- ========== ====== ============== -->
	<!-- 2022. 8. 10.} 고정현 최초작성 -->
	<!-- Copyright (c) 2022 by DDIT All right reserved -->
	<insert id="insertAttatch" parameterType="AttatchVO">
		<selectKey keyProperty="attatchNum,attatchPlace"
			resultType="AttatchVO" order="BEFORE">
			SELECT
			fn_create_num(to_char(sysdate,'yymmdd'), 'TB_ATTATCH', 'ATTATCH_NUM',
			UPPER('ATTA')) AS ATTATCH_NUM ,
			fn_create_num(to_char(sysdate,'yymmdd'), 'TB_COOFORM',
			'COO_FORM_NUM', UPPER('COFO')) AS ATTATCH_PLACE
			FROM DUAL
		</selectKey>
		INSERT INTO TB_ATTATCH
		(
		ATTATCH_NUM,
		ATTATCH_ORDER,
		ATTATCH_PLACE,
		FILE_PATH,
		ORIGIN_NM,
		SAVE_NM,
		ATTATCH_TYPE,
		ATTATCH_SIZE,
		UPLOADER_ID
		)
		VALUES(
		#{attatchNum},
		1,
		#{attatchPlace},
		#{filePath},
		#{originNm},
		#{saveNm},
		#{attatchType},
		#{attatchSize},
		#{uploaderId}
		)
	</insert>
	
	<insert id="insertReplyAttatch" parameterType="AttatchVO">
	<selectKey keyProperty="attatchNum,attatchPlace"
			resultType="AttatchVO" order="BEFORE">
			SELECT
			fn_create_num(to_char(sysdate,'yymmdd'), 'TB_ATTATCH', 'ATTATCH_NUM',
			UPPER('ATTA')) AS ATTATCH_NUM ,
			fn_create_num(to_char(sysdate,'yymmdd'), 'TB_WORK_REPLY',
			'WO_REPLY_NUM', UPPER('WORE')) AS ATTATCH_PLACE
			FROM DUAL
			
		</selectKey>
		INSERT INTO TB_ATTATCH
		(
		ATTATCH_NUM,
		ATTATCH_ORDER,
		ATTATCH_PLACE,
		FILE_PATH,
		ORIGIN_NM,
		SAVE_NM,
		ATTATCH_TYPE,
		ATTATCH_SIZE,
		UPLOADER_ID
		)
		VALUES(
		#{attatchNum},
		1,
		#{attatchPlace},
		#{filePath},
		#{originNm},
		#{saveNm},
		#{attatchType},
		#{attatchSize},
		#{uploaderId}
		)
	</insert>
	
	<insert id="createAttachNumReplyAttatch" parameterType="AttatchVO">
	<selectKey keyProperty="attatchNum"
			resultType="AttatchVO" order="BEFORE">
			SELECT
			fn_create_num(to_char(sysdate,'yymmdd'), 'TB_ATTATCH', 'ATTATCH_NUM',
			UPPER('ATTA')) AS ATTATCH_NUM 
			FROM DUAL
			
		</selectKey>
		INSERT INTO TB_ATTATCH
		(
		ATTATCH_NUM,
		ATTATCH_ORDER,
		ATTATCH_PLACE,
		FILE_PATH,
		ORIGIN_NM,
		SAVE_NM,
		ATTATCH_TYPE,
		ATTATCH_SIZE,
		UPLOADER_ID
		)
		VALUES(
		#{attatchNum},
		1,
		#{attatchPlace},
		#{filePath},
		#{originNm},
		#{saveNm},
		#{attatchType},
		#{attatchSize},
		#{uploaderId}
		)
	</insert>


	<select id="findAttachNum" parameterType="String" resultType="String">
	SELECT ATTATCH_NUM
	FROM TB_ATTATCH
	WHERE ATTATCH_PLACE =#{parentWoReplyNum}
	</select>
	<insert id="insertIssueReplyAttatch" parameterType="AttatchVO">
	<selectKey keyProperty="attatchNum,attatchPlace"
			resultType="AttatchVO" order="BEFORE">
			SELECT
			fn_create_num(to_char(sysdate,'yymmdd'), 'TB_ATTATCH', 'ATTATCH_NUM',
			UPPER('ATTA')) AS ATTATCH_NUM ,
				fn_create_num(to_char(sysdate,'yymmdd'), 'TB_ISSUE_REPLY',
			'ISSUE_REPLY_NUM', UPPER('ISRE')) AS ATTATCH_PLACE
			FROM DUAL
		</selectKey>
		INSERT INTO TB_ATTATCH
		(
		ATTATCH_NUM,
		ATTATCH_ORDER,
		ATTATCH_PLACE,
		FILE_PATH,
		ORIGIN_NM,
		SAVE_NM,
		ATTATCH_TYPE,
		ATTATCH_SIZE,
		UPLOADER_ID
		)
		VALUES(
		#{attatchNum},
		1,
		#{attatchPlace},
		#{filePath},
		#{originNm},
		#{saveNm},
		#{attatchType},
		#{attatchSize},
		#{uploaderId}
		)
	</insert>
	
	<update id="modifyIssueReplyAttatch" parameterType="AttatchVO">
		UPDATE TB_ATTATCH
		SET
		FILE_PATH=#{filePath},
		ORIGIN_NM=#{originNm},
		SAVE_NM=#{saveNm},
		ATTATCH_TYPE=#{attatchType},
		ATTATCH_SIZE=#{attatchSize}
        WHERE ATTATCH_NUM=#{attatchNum}
	</update>
	
	<insert id="insertGitAttatch" parameterType="AttatchVO">
		<selectKey keyProperty="attatchNum" resultType="String"
			order="BEFORE">
			SELECT fn_create_num(to_char(sysdate,'yymmdd'), 'TB_ATTATCH',
			'ATTATCH_NUM', UPPER('ATTA'))
			FROM DUAL
		</selectKey>
		INSERT INTO TB_ATTATCH
		(
		ATTATCH_NUM,
		ATTATCH_ORDER,
		ATTATCH_PLACE,
		FILE_PATH,
		ORIGIN_NM,
		SAVE_NM,
		ATTATCH_TYPE,
		ATTATCH_SIZE,
		UPLOADER_ID
		)
		VALUES(
		#{attatchNum},
		#{attatchOrder},
		#{attatchPlace},
		#{filePath},
		#{originNm},
		#{saveNm},
		#{attatchType},
		#{attatchSize},
		#{uploaderId}
		)
	</insert>

	<select id="createFolderNum" resultType="AttatchVO">
		SELECT
		fn_create_num(to_char(sysdate,'yymmdd'), 'TB_ATTATCH', 'ATTATCH_NUM',
		UPPER('ATTA')) AS ATTATCH_NUM ,
		fn_create_num(to_char(sysdate,'yymmdd'), 'TB_WFOLDER_FILE',
		'WFOLDER_FILE_NUM', UPPER('WFFL')) AS ATTATCH_PLACE
		FROM DUAL
	</select>

	<insert id="insertFolderAttatch" parameterType="AttatchVO">
		INSERT INTO TB_ATTATCH
		(
		ATTATCH_NUM,
		ATTATCH_ORDER,
		ATTATCH_PLACE,
		FILE_PATH,
		ORIGIN_NM,
		SAVE_NM,
		ATTATCH_TYPE,
		ATTATCH_SIZE,
		UPLOADER_ID
		)
		VALUES(
		#{attatchNum},
		#{attatchOrder},
		#{attatchPlace},
		#{filePath},
		#{originNm},
		#{saveNm},
		#{attatchType},
		#{attatchSize},
		#{uploaderId}
		)
	</insert>
	
	

	<select id="selectDownload" parameterType="String"
		resultType="AttatchVO">
		SELECT *
		FROM TB_ATTATCH
		WHERE ATTATCH_NUM = #{attatchNum}
	</select>

	<select id="selectAttatchFile" parameterType="AttatchVO"
		resultType="AttatchVO">
		SELECT *
		FROM TB_ATTATCH
		WHERE ATTATCH_NUM = #{attatchNum}
		AND ATTATCH_ORDER = #{attatchOrder}
		AND ATTATCH_PLACE = #{attatchPlace}
	</select>
	
	<select id="selectAttatchFileList"
	parameterType="AttatchVO"
		resultType="AttatchVO" >
		SELECT *
		FROM TB_ATTATCH
		WHERE ATTATCH_NUM = #{attatchNum}
		AND ATTATCH_PLACE = #{attatchPlace}
		</select>

	<delete id="deleteAttatchFile" parameterType="AttatchVO">
		DELETE FROM
		TB_ATTATCH
		WHERE ATTATCH_NUM = #{attatchNum}
		AND ATTATCH_ORDER = #{attatchOrder}
		AND ATTATCH_PLACE = #{attatchPlace}
	</delete>
	
	<delete id="deleteAttatchReplaceFile" parameterType="AttatchVO">
		DELETE FROM
		TB_ATTATCH
		WHERE ATTATCH_PLACE = #{attatchPlace}
	</delete>
	
	<update id="updateAttatch" parameterType="AttatchVO">
		UPDATE TB_ATTATCH
		SET DOWN_NUMBER = DOWN_NUMBER + 1
		WHERE ATTATCH_NUM = #{attatchNum}
		AND ATTATCH_ORDER = #{attatchOrder}
	</update>

	<select id="multiDownload" parameterType="AttatchVO"
		resultType="AttatchVO">
		SELECT *
		FROM TB_ATTATCH
		WHERE ATTATCH_NUM = #{attatchNum}
		AND ATTATCH_ORDER = #{attatchOrder}
	</select>

	<insert parameterType="MyBlogPostVO" id="insertBlogAttatches">
		<selectKey keyProperty="attatchNum,postNum"
			resultType="MyBlogPostVO" order="BEFORE">
			<if test="@org.apache.commons.lang3.StringUtils@isBlank(postNum)">
				SELECT
				(SELECT
				FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_ATTATCH', 'ATTATCH_NUM', UPPER('ATTA'))
				FROM DUAL) AS ATTATCH_NUM
				, (SELECT FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_BLOG_POST',
				'POST_NUM', UPPER('BLPO'))
				FROM DUAL) AS POST_NUM
				FROM DUAL
			</if>
			<if
				test="@org.apache.commons.lang3.StringUtils@isNotBlank(postNum)">
				SELECT
				(SELECT
				FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_ATTATCH', 'ATTATCH_NUM', UPPER('ATTA'))
				FROM DUAL) AS ATTATCH_NUM
				, (SELECT POST_NUM FROM TB_BLOG_POST WHERE POST_NUM=#{postNum}) AS
				POST_NUM
				FROM DUAL
			</if>
		</selectKey>
		INSERT ALL
		<foreach index="idx" item="attatch" collection="attatchList">
			INTO TB_ATTATCH (
			ATTATCH_NUM
			, ATTATCH_ORDER
			, ATTATCH_PLACE
			, FILE_PATH
			, ORIGIN_NM
			, SAVE_NM
			, ATTATCH_TYPE
			, ATTATCH_SIZE
			, UPLOADER_ID
			) VALUES (
			#{attatchNum}
			, #{idx}
			, #{postNum}
			,
			#{attatch.filePath,jdbcType=VARCHAR}
			,
			#{attatch.originNm,jdbcType=VARCHAR}
			,
			#{attatch.saveNm,jdbcType=VARCHAR}
			,
			#{attatch.attatchType,jdbcType=VARCHAR}
			, #{attatch.attatchSize,jdbcType=NUMERIC}
			, #{blogerId,jdbcType=VARCHAR})
		</foreach>
		SELECT * FROM DUAL
	</insert>

	<select id="selectAttatchNum" parameterType="String">
		SELECT DISTINCT
		ATTATCH_NUM
		FROM TB_ATTATCH
		WHERE ATTATCH_PLACE = #{attatchPlace}
	</select>

	<insert parameterType="NoticeVO" id="insertNoticeAttatches">
		<selectKey keyProperty="attatchNum,notiNum"
			resultType="NoticeVO" order="BEFORE">
			<if test="@org.apache.commons.lang3.StringUtils@isBlank(notiNum)">
				SELECT
				(SELECT
				FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_ATTATCH', 'ATTATCH_NUM', UPPER('ATTA'))
				FROM DUAL) AS ATTATCH_NUM
				, (SELECT FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_NOTICE',
				'NOTI_NUM', UPPER('NOTI'))
				FROM DUAL) AS NOTI_NUM
				FROM DUAL
			</if>
			<if
				test="@org.apache.commons.lang3.StringUtils@isNotBlank(notiNum)">
				SELECT
				(SELECT
				FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_ATTATCH', 'ATTATCH_NUM', UPPER('ATTA'))
				FROM DUAL) AS ATTATCH_NUM
				, (SELECT NOTI_NUM FROM TB_NOTICE WHERE NOTI_NUM=#{notiNum}) AS
				NOTI_NUM
				FROM DUAL
			</if>
		</selectKey>
		INSERT ALL
		<foreach index="idx" item="attatch" collection="attatchList">
			INTO TB_ATTATCH (
			ATTATCH_NUM
			, ATTATCH_ORDER
			, ATTATCH_PLACE
			, FILE_PATH
			, ORIGIN_NM
			, SAVE_NM
			, ATTATCH_TYPE
			, ATTATCH_SIZE
			, UPLOADER_ID
			) VALUES (
			#{attatchNum}
			, #{idx}
			, #{notiNum}
			,
			#{attatch.filePath,jdbcType=VARCHAR}
			,
			#{attatch.originNm,jdbcType=VARCHAR}
			,
			#{attatch.saveNm,jdbcType=VARCHAR}
			,
			#{attatch.attatchType,jdbcType=VARCHAR}
			, #{attatch.attatchSize,jdbcType=NUMERIC}
			, #{writerId,jdbcType=VARCHAR})
		</foreach>
		SELECT * FROM DUAL
	</insert>

	<!-- freeBoardAttatch -->
	<insert parameterType="FreeBoardVO"
		id="insertFreeBoardAttatches">
		<selectKey keyProperty="attatchNum,freeNum"
			resultType="FreeBoardVO" order="BEFORE">
			<if test="@org.apache.commons.lang3.StringUtils@isBlank(freeNum)">
				SELECT
				(SELECT
				FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_ATTATCH', 'ATTATCH_NUM', UPPER('ATTA'))
				FROM DUAL) AS ATTATCH_NUM
				, (SELECT FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_FREEBOARD',
				'FREE_NUM', UPPER('FREE'))
				FROM DUAL) AS FREE_NUM
				FROM DUAL
			</if>
			<if
				test="@org.apache.commons.lang3.StringUtils@isNotBlank(freeNum)">
				SELECT
				(SELECT
				FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_ATTATCH', 'ATTATCH_NUM', UPPER('ATTA'))
				FROM DUAL) AS ATTATCH_NUM
				, (SELECT FREE_NUM FROM TB_FREEBOARD WHERE FREE_NUM=#{freeNum}) AS
				FREE_NUM
				FROM DUAL
			</if>
		</selectKey>
		INSERT ALL
		<foreach index="idx" item="attatch" collection="attatchList">
			INTO TB_ATTATCH (
			ATTATCH_NUM
			, ATTATCH_ORDER
			, ATTATCH_PLACE
			, FILE_PATH
			, ORIGIN_NM
			, SAVE_NM
			, ATTATCH_TYPE
			, ATTATCH_SIZE
			, UPLOADER_ID
			) VALUES (
			#{attatchNum}
			, #{idx}
			, #{freeNum}
			,
			#{attatch.filePath,jdbcType=VARCHAR}
			,
			#{attatch.originNm,jdbcType=VARCHAR}
			,
			#{attatch.saveNm,jdbcType=VARCHAR}
			,
			#{attatch.attatchType,jdbcType=VARCHAR}
			, #{attatch.attatchSize,jdbcType=NUMERIC}
			, #{writerId,jdbcType=VARCHAR})
		</foreach>
		SELECT * FROM DUAL
	</insert>
	<!-- freeBoardAttatch -->
	
	<!-- cooBoardAttatch -->
	<insert parameterType="CooBoardVO"
		id="insertCooBoardAttatches">
		<selectKey keyProperty="attatchNum,cooNum"
			resultType="CooBoardVO" order="BEFORE">
			<if test="@org.apache.commons.lang3.StringUtils@isBlank(cooNum)">
				SELECT
				  (SELECT FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_ATTATCH', 'ATTATCH_NUM', UPPER('ATTA'))FROM DUAL) AS ATTATCH_NUM
				, (SELECT FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_COOBOARD','COO_NUM', UPPER('COBO'))FROM DUAL) AS COO_NUM
				FROM DUAL
			</if>
			<if
				test="@org.apache.commons.lang3.StringUtils@isNotBlank(cooNum)">
				SELECT
				(SELECT
				FN_CREATE_NUM(TO_CHAR(SYSDATE,'YYMMDD'), 'TB_ATTATCH', 'ATTATCH_NUM', UPPER('ATTA'))
				FROM DUAL) AS ATTATCH_NUM
				, (SELECT COO_NUM FROM TB_COOBOARD WHERE COO_NUM=#{cooNum}) AS COO_NUM
				FROM DUAL
			</if>
		</selectKey>
		INSERT ALL
		<foreach index="idx" item="attatch" collection="attatchList">
			INTO TB_ATTATCH (
			ATTATCH_NUM
			, ATTATCH_ORDER
			, ATTATCH_PLACE
			, FILE_PATH
			, ORIGIN_NM
			, SAVE_NM
			, ATTATCH_TYPE
			, ATTATCH_SIZE
			, UPLOADER_ID
			) VALUES (
			#{attatchNum}
			, #{idx}
			, #{cooNum}
			,
			#{attatch.filePath,jdbcType=VARCHAR}
			,
			#{attatch.originNm,jdbcType=VARCHAR}
			,
			#{attatch.saveNm,jdbcType=VARCHAR}
			,
			#{attatch.attatchType,jdbcType=VARCHAR}
			, #{attatch.attatchSize,jdbcType=NUMERIC}
			, #{writerId,jdbcType=VARCHAR})
		</foreach>
		SELECT * FROM DUAL
	</insert>
	
	<select id="selectAttatch" parameterType="AttatchVO" resultType="AttatchVO">
		SELECT *
		  FROM TB_ATTATCH
		 WHERE ATTATCH_PLACE = #{attatchPlace}
	</select>
	<!-- cooBoardAttatch -->
</mapper>